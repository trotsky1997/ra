# Tasks

- [x] Add webfetch tool implementations with argv-safe npm execution.
- [x] Register `webfetch_fetch` and `webfetch_crawl` in the default built-in catalog.
- [x] Update README, `spec/tools.md`, and init/config examples.
- [x] Add integration tests for registration, argv mapping, output truncation, and missing-npm structured errors.
- [x] Run focused and full test suites.
