    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.71s
     Running `target/debug/gen-schema`
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RaConfig",
  "description": "The whole config file. All sections are optional so a minimal `version = 1` document is a valid Ra config.",
  "type": "object",
  "properties": {
    "a2a": {
      "$ref": "#/definitions/A2aSection"
    },
    "agents_md": {
      "$ref": "#/definitions/AgentsMdSection"
    },
    "graphify": {
      "$ref": "#/definitions/GraphifySection"
    },
    "hooks": {
      "$ref": "#/definitions/HooksSection"
    },
    "mcp": {
      "$ref": "#/definitions/McpSection"
    },
    "model": {
      "$ref": "#/definitions/ModelSelectorSection"
    },
    "models": {
      "description": "Array-of-tables: `[[models]]` entries.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/ModelEntrySection"
      }
    },
    "obs": {
      "$ref": "#/definitions/ObsSection"
    },
    "openlsp": {
      "$ref": "#/definitions/OpenlspSection"
    },
    "openspec": {
      "$ref": "#/definitions/OpenSpecSection"
    },
    "prompts": {
      "$ref": "#/definitions/PromptsSection"
    },
    "resources": {
      "$ref": "#/definitions/ResourcesSection"
    },
    "rtk": {
      "$ref": "#/definitions/RtkSection"
    },
    "run": {
      "$ref": "#/definitions/RunSection"
    },
    "session": {
      "$ref": "#/definitions/SessionSection"
    },
    "skills": {
      "$ref": "#/definitions/SkillsSection"
    },
    "tools": {
      "$ref": "#/definitions/ToolsSection"
    },
    "version": {
      "description": "Schema version. Currently 1; future breaks bump it.",
      "default": 1,
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    }
  },
  "additionalProperties": false,
  "definitions": {
    "A2aRemoteAgent": {
      "type": "object",
      "required": [
        "name",
        "url"
      ],
      "properties": {
        "auth": {
          "anyOf": [
            {
              "$ref": "#/definitions/AuthSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "name": {
          "type": "string"
        },
        "url": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "A2aSection": {
      "type": "object",
      "properties": {
        "remote_agents": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/A2aRemoteAgent"
          }
        },
        "serve": {
          "$ref": "#/definitions/A2aServeSection"
        }
      },
      "additionalProperties": false
    },
    "A2aServeSection": {
      "type": "object",
      "properties": {
        "auth": {
          "description": "Optional Bearer token gate. When set, every JSON-RPC, REST, and gRPC request must carry `Authorization: Bearer <env-value>` (or the gRPC `authorization` metadata key). The well-known agent-card endpoint is exempted so unauthenticated callers can discover the auth scheme.",
          "anyOf": [
            {
              "$ref": "#/definitions/AuthSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "grpc_port": {
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0.0
        },
        "http_port": {
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AgentsMdSection": {
      "description": "`[agents_md]` — auto-discover AGENTS.md files (https://agents.md/). We walk the cwd up to the git root and concatenate every AGENTS.md we find, \"nearest-file-wins\" by ordering them root→leaf so deeper files appear later in the system prompt and override.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Default true. Set false to disable AGENTS.md auto-discovery (some projects ship one but want Ra to ignore it).",
          "default": true,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "AuthSpec": {
      "type": "object",
      "properties": {
        "bearer_env": {
          "description": "Env var holding a Bearer token to send as `Authorization`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "GraphifySection": {
      "description": "`[graphify]` — native [Graphify](https://github.com/safishamsi/graphify) support. When enabled, Ra targets a project's `graphify-out/graph.json` (walking cwd → git root), treats Graphify as an agent-owned R2A graph workflow, and registers ensure/impact/update plus native graph query tools.",
      "type": "object",
      "properties": {
        "discover": {
          "description": "Auto-discover `graphify-out/graph.json` using cwd → git-root walking. Default true. Set false when you only want an explicit `path`.",
          "default": true,
          "type": "boolean"
        },
        "enabled": {
          "description": "Default true. Set false to disable Graphify discovery and tools.",
          "default": true,
          "type": "boolean"
        },
        "path": {
          "description": "Override the graph path or Graphify output directory. `~` is expanded, relative paths resolve against cwd, and directories imply `graph.json`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "Hook": {
      "type": "object",
      "required": [
        "command"
      ],
      "properties": {
        "command": {
          "description": "Shell command to invoke. Receives the event JSON on stdin.",
          "type": "string"
        },
        "matcher": {
          "description": "Regex matched against the tool name (PreToolUse/PostToolUse) or the user message text (UserPromptSubmit). Default `.*`.",
          "default": ".*",
          "type": "string"
        },
        "run_async": {
          "description": "Fire-and-forget: don't wait for response.",
          "default": false,
          "type": "boolean"
        },
        "timeout": {
          "description": "Hard timeout in seconds. Default 60. Accepted under `timeout` (Claude Code spelling) or the legacy `timeout_s`.",
          "default": 60.0,
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false
    },
    "HooksSection": {
      "type": "object",
      "properties": {
        "PostToolUse": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Hook"
          }
        },
        "PreToolUse": {
          "description": "PreToolUse hooks (Claude Code spec name). Also accepted under the legacy snake_case alias `pre_tool_use`.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/Hook"
          }
        },
        "Stop": {
          "description": "Claude Code calls this `Stop` (assistant finished). Legacy alias `agent_end` still works.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/Hook"
          }
        },
        "UserPromptSubmit": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Hook"
          }
        }
      },
      "additionalProperties": false
    },
    "McpSection": {
      "type": "object",
      "properties": {
        "servers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/McpServer"
          }
        }
      },
      "additionalProperties": false
    },
    "McpServer": {
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "args": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "auth": {
          "anyOf": [
            {
              "$ref": "#/definitions/AuthSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "command": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "cwd": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "env": {
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "name": {
          "type": "string"
        },
        "transport": {
          "description": "`stdio` | `http`. Default `stdio`.",
          "default": "stdio",
          "type": "string"
        },
        "url": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ModelEntrySection": {
      "description": "One entry in `[[models]]`. Mirrors HCP's `[model]` shape but lifted into an array so Ra can advertise a catalog at session/new time.",
      "type": "object",
      "required": [
        "backend",
        "model_id",
        "name"
      ],
      "properties": {
        "api_key": {
          "description": "API key inline. Strongly discouraged; prefer `api_key_env`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "api_key_env": {
          "description": "Env var holding the API key. Required unless `api_key` is given.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "backend": {
          "description": "`openai` | `anthropic` | `google` | `deepseek` | `ollama` | `groq` | `xai`. Mapped to the graniet/llm `LLMBackend` enum.",
          "type": "string"
        },
        "base_url": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "model_id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "ModelSelectorSection": {
      "type": "object",
      "properties": {
        "default": {
          "description": "Name of the entry in `[[models]]` to use by default.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ObsSection": {
      "type": "object",
      "properties": {
        "backend": {
          "description": "`stderr` | `file` | `otel` | `none`. Maps to `RA_OBS_BACKEND`. Empty string treated as unset.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "otel_endpoint": {
          "description": "OTLP endpoint when backend=otel. Equivalent of `OTEL_EXPORTER_OTLP_ENDPOINT`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "OpenSpecSection": {
      "description": "`[openspec]` — native [OpenSpec](https://github.com/Fission-AI/OpenSpec) discovery. When enabled (the default), Ra locates the nearest `openspec/` directory (walking cwd → git root, same as AGENTS.md) and folds a high-signal catalog of its capability specs and active changes into the system prompt with progressive disclosure. Ra is the *consumer* of the convention; it does not reimplement the `openspec` CLI.",
      "type": "object",
      "properties": {
        "agent_own": {
          "description": "Default true. When a project is discovered, fold a short *agent-own* spec-driven-development playbook into the system prompt: how to drive the `openspec` CLI non-interactively (`init --tools`, `new change`, the `status`/`instructions --json` state machine, `validate --strict` self-correction, `archive -y`) with no human in the loop. When no `openspec/` exists yet, a short bootstrap hint is folded in instead, so the agent can adopt the convention itself. Set false to surface only the catalog.",
          "default": true,
          "type": "boolean"
        },
        "enabled": {
          "description": "Default true. Set false to disable OpenSpec auto-discovery even when an `openspec/` directory is present.",
          "default": true,
          "type": "boolean"
        },
        "path": {
          "description": "Override the directory Ra discovers. By default Ra walks cwd up to the git root looking for a folder literally named `openspec`; set this to point at a non-standard location. `~` is expanded and relative paths resolve against the cwd.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "OpenlspSection": {
      "description": "`[openlsp]` — native [openlsp](https://github.com/trotsky1997/openlsp) integration. When enabled (the default), Ra registers a built-in `lsp` tool that dispatches openlsp command envelopes via a child process. The tool is silently omitted if no openlsp binary can be resolved.",
      "type": "object",
      "properties": {
        "binary": {
          "description": "Override the openlsp binary path. By default Ra tries `openlsp` on PATH, then `bunx openlsp` if `bun` is available.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "description": "Default true. Set false to disable the lsp built-in tool even when openlsp is installed.",
          "default": true,
          "type": "boolean"
        },
        "timeout": {
          "description": "Per-call timeout in seconds. Default 30.0.",
          "default": 30.0,
          "type": "number",
          "format": "double"
        },
        "workspace_root": {
          "description": "Workspace root passed to openlsp via `--workspace-root`. Defaults to the session cwd when unset.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "PromptsSection": {
      "type": "object",
      "properties": {
        "enabled": {
          "default": true,
          "type": "boolean"
        },
        "paths": {
          "description": "Glob patterns. Each match becomes a slash command named after the file stem.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "ResourcesSection": {
      "description": "`[resources]` — extra plain-text files concatenated into the system prompt verbatim. Less structured than `[skills]`; useful for ad-hoc system prompts or repo conventions kept in a non-AGENTS.md file.",
      "type": "object",
      "properties": {
        "append_system_prompt_paths": {
          "description": "Additional files appended after the primary one.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "system_prompt_path": {
          "description": "Optional path to a single primary system prompt file.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "RtkMode": {
      "oneOf": [
        {
          "description": "Use rtk when it's on PATH; transparent fallthrough otherwise.",
          "type": "string",
          "enum": [
            "auto"
          ]
        },
        {
          "description": "Require rtk to be available; warn at startup if missing.",
          "type": "string",
          "enum": [
            "on"
          ]
        },
        {
          "description": "Never invoke rtk even if it's installed.",
          "type": "string",
          "enum": [
            "off"
          ]
        }
      ]
    },
    "RtkSection": {
      "description": "`[rtk]` — native [RTK (Rust Token Killer)](https://github.com/rtk-ai/rtk) integration. When enabled, the BashTool runs every command through `rtk rewrite` before executing it. RTK rewrites well-known dev commands (git, cargo, pytest, docker, kubectl, …) into their token-compressed equivalents, often shaving 60–90% off the bytes fed back into the model context.",
      "type": "object",
      "properties": {
        "binary": {
          "description": "Override the rtk binary path. By default we look up `rtk` on PATH.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "mode": {
          "description": "`auto` (default): use rtk if it's on PATH, otherwise transparent. `true` / `\"on\"`: require rtk; warn if missing. `false` / `\"off\"`: disable even if rtk is installed.",
          "allOf": [
            {
              "$ref": "#/definitions/RtkMode"
            }
          ]
        },
        "ultra_compact": {
          "description": "Pass `--ultra-compact` to `rtk rewrite` for even tighter output.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "RunSection": {
      "type": "object",
      "properties": {
        "banner": {
          "description": "Print the Ra banner on stderr at startup. Default true.",
          "default": true,
          "type": "boolean"
        },
        "data_dir": {
          "description": "Root for ATIF trajectories, ATOF logs, etc. Defaults to `dirs::data_local_dir() / \"ra\"`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "default_cwd": {
          "description": "Default working directory used for session bucketing when an ACP client doesn't pin one. Defaults to `std::env::current_dir()`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "SessionSection": {
      "type": "object",
      "properties": {
        "mode": {
          "description": "`default` | `plan` | `ask`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "trajectory_dir": {
          "description": "Override for the trajectory dir. Otherwise `<data_dir>/sessions`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "SkillsSection": {
      "type": "object",
      "properties": {
        "discover": {
          "description": "Auto-discover skills from the standard skills.sh layouts: `./.ra/skills/`, `~/.ra/skills/`, plus the shared cross-agent folders `.claude/skills/`, `.cursor/skills/`, `.codex/skills/`, `.kiro/skills/`, `.opencode/skills/` and the catalog-style `skills/`, `skills/.curated/`, `skills/.experimental/`, `skills/.system/`. Default true so `npx skills add` and bare project layouts Just Work.",
          "default": true,
          "type": "boolean"
        },
        "enabled": {
          "description": "Master switch. Default true.",
          "default": true,
          "type": "boolean"
        },
        "paths": {
          "description": "Extra glob patterns expanded against `~` and the cwd. Each match is loaded as a SKILL.md, on top of (and de-duplicated against) whatever `discover` finds.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "ToolsSection": {
      "type": "object",
      "properties": {
        "builtin": {
          "description": "Builtin tool allow-list. Empty Vec = all enabled (default). Non-empty: only the listed names ship to the LLM.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
